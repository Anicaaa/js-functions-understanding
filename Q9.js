//Q9

function myFunction(num1, num2) {
  console.log(num3);
}

const num1 = 10;
const num2 = 1;
const num3 = 20;

myFunction(num3, num1);

/* 
What will be logged out on the console when this code runs? Why?

Answer: It will console log 20.

Reason: The variable num3 is 20 and placed as a global scope.

Result: As expected
*/
